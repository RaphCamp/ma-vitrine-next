import { Inter, Rubik_Pixels } from 'next/font/google'
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});
export const rubik_pixels = Rubik_Pixels({
    subsets: ['latin'],
    display: 'swap',
    weight: '400'
});